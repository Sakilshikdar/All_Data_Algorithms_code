#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;
vector<string> g;
int main()
{
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < n; i++)
    {
        string s;
        cin >> s;
        g.push_back(s);
    }

    for (int i = 0; i < m; i++)
    {
        for (int j = 0; j < m; j++)
        {
        }
    }
    return 0;
}